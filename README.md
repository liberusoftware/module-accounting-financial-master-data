# Liberu Accounting Financial Master Data

The authoritative package boundary for accounting counterparties and setup
data. It owns customer/supplier parties, catalog items and services, tax
profiles, payment terms, addresses, bank-detail references, lifecycle state,
and scoped duplicate detection. It never imports the host application's
`App\\` models.
